# DOM-Only State Inventory

<!-- Not an implementation plan. A catalogue produced by plans/dom-only-state-audit.md. -->

This is a read-only audit: it catalogues state that a future release-and-rebuild
feature would lose or dangle, and prescribes no code change. Verdicts on
Table A rows are advisory for the follow-up plan that designs the release API.

## Verified assumptions

- **Lazy re-materialization.** `Component.getElement(createIfMissing)` returns
  the cached `_element` handle if set; otherwise, when `createIfMissing` is
  true, it calls `render()` and caches the result. A component with no
  element and the same id can therefore be re-materialized on demand.
  [Component.ts:863-876](packages/lib/src/typescript/lib/core/Component.ts#L863-L876)
- **Id-keyed event routing.** `Event` installs exactly one window-level
  capture listener per event type ([Event.ts:101](packages/lib/src/typescript/lib/core/Event.ts#L101)) and dispatches by
  interning the browser target into a `Handle`, resolving `DOM.source.getId(handle)`,
  and walking up the handle chain matching against id-keyed listener maps —
  never against a live element reference
  ([Event.ts:139-186](packages/lib/src/typescript/lib/core/Event.ts#L139-L186)). A listener registered through `Event`
  therefore survives a rebuild automatically, as long as the fresh element
  carries the same id (which `init()` sets unconditionally — see below).
- **What `init()` already replays onto a fresh element**
  ([Component.ts:5245-5297](packages/lib/src/typescript/lib/core/Component.ts#L5245-L5297)): the id (`DOM.sink.setId`), the class
  name (`addClass`), the buffered inline style (`_inlineStyle.attach`, replaying
  every queued `setElementStyle`/`setElementCSSRule` write), the `_attributes`
  map, `_disabledAttribute`, `_options.attributes`, ARIA (`_aria.applyToElement`),
  `applyStyle(element)`, and re-appending each existing child component's
  element (via `this.getComponents()`, i.e. `_components` — **not** any raw
  child element a subclass manages directly). Anything on this list is
  `accept-loss` by default (already handled) — the interesting rows are what
  is *not* on it, or what is written on top of it by a subclass's own
  `init()`/`render()` override.
- Two claims from the plan's scoping needed no re-derivation, confirmed
  unchanged: no `ResizeObserver`/`IntersectionObserver`/`MutationObserver`
  appears anywhere in the library, and there is no `<details>`, `<dialog>`,
  or native `checked` state (`Checkbox` keeps selected/indeterminate in
  `_options` and draws its own mark).

---

## Table A — state a rebuild would lose

Three worked rows, verified in the plan and used verbatim:

| Class | Site | State | Written by | Read by | JS mirror? | Verdict | Notes |
|---|---|---|---|---|---|---|---|
| `TextInput` | [TextInput.ts:610](packages/lib/src/typescript/lib/component/input/TextInput.ts#L610) | Text selection range | `DOM.sink.setSelectionRange` | nothing — write-only | no | `replay` | probe: no `setSelectionRange` on the rebuilt element. manual-verify: caret position after rebuild while focused |
| `TextInput` | [TextInput.ts:695](packages/lib/src/typescript/lib/component/input/TextInput.ts#L695) | Input `value` | `DOM.sink.setValue` from `init()` | [TextInput.ts:134](packages/lib/src/typescript/lib/component/input/TextInput.ts#L134) `DOM.source.getValue` | yes — `_options.text` | `accept-loss` | already replayed by `init()`; probe confirms |
| `WebGLCanvas` | [WebGLCanvas.ts:192](packages/lib/src/typescript/lib/component/display/WebGLCanvas.ts#L192) | GL context + every GPU resource built against it | `DOM.sink.getContext` | `_gl` field | no — the context *is* the state | `opt-out` | a new element yields a new context; buffers, textures and shaders compiled against the old one are unrecoverable |

### core/ and overlay/ — shared machinery

| Class | Site | State | Written by | Read by | JS mirror? | Verdict | Notes |
|---|---|---|---|---|---|---|---|
| `Component` | [Component.ts:3303-3339](packages/lib/src/typescript/lib/core/Component.ts#L3303-L3339) (`setScrollLeft`/`setScrollTop`) | native `scrollLeft`/`scrollTop` | `DOM.sink.apply({scrollLeft/scrollTop})` | `getScrollLeft`/`getScrollTop` (cached) | yes — `_scrollLeft`/`_scrollTop` | `replay` | probe: no scroll-carrying `apply` patch on the rebuilt element. Two call sites bypass the setter entirely, writing `DOM.sink.apply(el, {scrollTop})` directly and never updating the mirror: [PickerColumn.ts:389](packages/lib/src/typescript/lib/component/input/PickerColumn.ts#L389), [AbstractSelectableList.ts:2010-2012](packages/lib/src/typescript/lib/component/list/AbstractSelectableList.ts#L2010-L2012) (see their own rows below — the two sites reach different verdicts on context). manual-verify: scroll position after rebuild |
| `Component` | [Component.ts:4237](packages/lib/src/typescript/lib/core/Component.ts#L4237) / [:4254](packages/lib/src/typescript/lib/core/Component.ts#L4254) (`focus`/`blur`) | native focus | `DOM.sink.focus`/`blur` | nothing cached | no | `replay` | restoring focus re-triggers native `focus()`, which scrolls `overflow:hidden` ancestors unless `preventScroll` is passed (see `feedback_focus_scroll_pollution` project note). `Dialog._previousFocus` and the global `FocusHistory` module both hold `Handle`s that would need re-resolving by id after a rebuild — see Table B. manual-verify: re-focus after rebuild does not scroll an `overflow:hidden` ancestor |
| `Animation` (utility, not a `Component`) | [Animation.ts:140](packages/lib/src/typescript/lib/core/Animation.ts#L140) / [:233](packages/lib/src/typescript/lib/core/Animation.ts#L233) | `transitionend` completion listener for an in-flight CSS transition | `DOM.sink.addListener(el, "transitionend", ...)` | fires the finish callback once | no | `accept-loss` | bound directly to the caller-supplied element, not routed through `Event`; if that element is released mid-transition the finish callback simply never fires — the same behaviour as the element being destroyed today, not a new risk introduced by release. manual-verify: releasing a component mid-transition doesn't throw and leaves the animation's promise/callback silently unresolved |
| `AbstractWindow` | [AbstractWindow.ts:1889-1908](packages/lib/src/typescript/lib/overlay/AbstractWindow.ts#L1889-L1908) (`render()` override) | the 8 resize-handle border components | direct `DOM.sink.appendChild`, bypassing `_components` | — | yes — `_borderComponents` | `accept-loss` | step-6 cross-check: `AbstractWindow`'s own `render()` override re-appends all 8 border components (via their own `getElement(true)`) on every render, so this self-heals without any base-machinery help. manual-verify: resize handles are present and functional after a rebuild |
| `RailHandle` | [RailHandle.ts:113-118](packages/lib/src/typescript/lib/overlay/RailHandle.ts#L113-L118) (`render()` override) | `.selected` class | direct `DOM.sink.apply({toggleClass})`, not `setElementCSSRule` | `isSelected()` (`_options.selected`) | yes | `accept-loss` | step-6 cross-check: `RailHandle`'s own `render()` override reapplies the class from the mirror on every render. manual-verify: selected visual state survives a rebuild |
| `Dialog` | [Dialog.ts:882-1101](packages/lib/src/typescript/lib/overlay/Dialog.ts#L882-L1101) (`focusFirst`, focus restore) | which element receives focus on open / regains it on close | `DOM.sink.focus` | nothing cached beyond `_previousFocus` (Table B) | partial — `_previousFocus` | `replay` | concrete instance of the generic `Component` focus row above; restoring depends on `_previousFocus` still resolving to a live element (see Table B). manual-verify: focus returns to the correct element after a dialog opened post-rebuild is closed |
| `Canvas` | [Canvas.ts:90-96](packages/lib/src/typescript/lib/component/display/Canvas.ts#L90-L96) (`_syncedWidth`/`_syncedHeight`/`_syncedDpr`) | cached backing-store sync guards | `syncBackingStore()` | `syncBackingStore()`'s unchanged-value short-circuit ([Canvas.ts:277-279](packages/lib/src/typescript/lib/component/display/Canvas.ts#L277-L279)) | yes — and that's the bug | `replay` | nothing resets these three on release, so after a rebuild `syncBackingStore()`'s guard sees "unchanged" and skips resizing the fresh canvas's backing store, which silently sits at the browser's 300×150 default. Compounds the `_ctx` staleness in Table B. manual-verify: a rebuilt `<canvas>` is sized correctly, not 300×150 |

### layout/ — layout-manager-injected structural children

| Class | Site | State | Written by | Read by | JS mirror? | Verdict | Notes |
|---|---|---|---|---|---|---|---|
| `Split` / `Border` / `Accordion` / `Tab` (layout managers) | [Split.ts:1033-1185](packages/lib/src/typescript/lib/layout/Split.ts#L1033-L1185), [Border.ts:382](packages/lib/src/typescript/lib/layout/Border.ts#L382), [Accordion.ts:1140-1686](packages/lib/src/typescript/lib/layout/Accordion.ts#L1140-L1686), [Tab.ts:927](packages/lib/src/typescript/lib/layout/Tab.ts#L927) | presence of gutter / header / wrapper components in the rebuilt container's DOM | direct `DOM.sink.appendChild` on `container.getElement()`, bypassing `_components` | — | the Component references themselves stay valid (they are not released) | `replay` | these are layout managers, not `Component`s — they inject structural children directly into the container's element outside `_components`, so `Component.init()`'s child-reappend loop never touches them. A container-level rebuild needs the layout manager's own build/relayout pass to re-run and re-append; nothing today triggers that automatically in response to a rebuild specifically (as opposed to an ordinary `doLayout`). This is the same shape as the `Panel` overlay-scrollbar/shadow rows below, one level up. manual-verify: after a container release+rebuild, confirm gutters/headers reappear on the next layout pass — behaviour likely differs per layout manager and needs a live check once the release API exists |

### component/input/, component/display/, component/editor/

| Class | Site | State | Written by | Read by | JS mirror? | Verdict | Notes |
|---|---|---|---|---|---|---|---|
| `Label` | [Label.ts:64-92](packages/lib/src/typescript/lib/component/input/Label.ts#L64-L92) | `for` attribute | direct `DOM.sink.apply`, not `setElementAttribute` | `getForId()` (`_forId`) | yes | `accept-loss` | `Label`'s own `render()` override reapplies `for` from `_forId` on every render. manual-verify: label-to-input association survives a rebuild |
| `AbstractCalendarDropdown` (`PickerDay`/`PickerNavButton`/`PickerMonthLabel`/`PickerCell`) | [AbstractCalendarDropdown.ts:195-392](packages/lib/src/typescript/lib/component/input/AbstractCalendarDropdown.ts#L195-L392), [PickerColumn.ts:266-274](packages/lib/src/typescript/lib/component/input/PickerColumn.ts#L266-L274) | `.disabled`/`.selected`/`.PickerDay`/`.PickerNavButton` classes | each subclass's own `render()` | `isDisabled()`/`isSelected()` | yes | `accept-loss` | every affected inner class re-applies its class list from a mirrored field in its own `render()` override. manual-verify: disabled/selected day styling survives a rebuild |
| `PickerColumn` | [PickerColumn.ts:351-392](packages/lib/src/typescript/lib/component/input/PickerColumn.ts#L351-L392) (write at 389) | `scrollTop` of the cell-list surface | direct `DOM.sink.apply({scrollTop})`, bypassing the mirror | none | no | `accept-loss` | transient; the caller (`openYearScroller`/`applyYearHighlight`) already re-invokes `scrollSelectedIntoView()` on every relevant state change, so a reset-to-0 on release self-corrects on the next interaction. manual-verify: year/time scroller lands on the right row after the next open following a rebuild |
| `Slider` | [Slider.ts:551](packages/lib/src/typescript/lib/component/input/Slider.ts#L551) / [:573](packages/lib/src/typescript/lib/component/input/Slider.ts#L573) | active pointer-capture gesture (`_draggingPointer`) | `DOM.sink.setPointerCapture`/`releasePointerCapture` | `_draggingPointer` (no public getter) | yes | `accept-loss` | transient drag-interaction state; destroying the captured element implicitly ends the OS-level capture. If the element died mid-drag without a `pointerup`/`pointercancel`/`lostpointercapture`, `_draggingPointer` could be left non-null, but the next `pointerdown` unconditionally overwrites it. manual-verify: dragging a slider whose element is released mid-gesture doesn't leave it stuck in a dragging state |
| `ComboBox` (`ComboBoxLabel`) | [ComboBox.ts:474-483](packages/lib/src/typescript/lib/component/input/ComboBox.ts#L474-L483) | the hosted `ListItemRenderer`'s element, appended as a raw (non-`_components`) child | `ComboBoxLabel.init()` | `_renderer` field | yes | `accept-loss` | `ComboBoxLabel`'s own `init()` override explicitly re-appends `this._renderer.getElement(true)` on the fresh element. manual-verify: the selected-item renderer is visible after a rebuild |
| `FileField` | [FileField.ts:122](packages/lib/src/typescript/lib/component/input/FileField.ts#L122) / [:135](packages/lib/src/typescript/lib/component/input/FileField.ts#L135) / [:146](packages/lib/src/typescript/lib/component/input/FileField.ts#L146) | the native, user-picked `FileList` | browser (OS picker) | `getValue()`/`getFiles()` — both re-read the *live* input lazily, no cache | no | `accept-loss` | the browser forbids programmatically re-populating a `<input type=file>` outside a user gesture, so even a captured `File[]` can't be written back to the element — but the `File` objects themselves are independently held once read. A release silently loses the current selection with no re-render mechanism today. Upgradable to `replay` for the *value*, not the native input state, by caching `Array.from(input.files)` in `onNativeChange()` (mirroring the existing `_droppedFiles` pattern) and exposing it as the accept-loss-free read path. manual-verify: `getFiles()` after a rebuild while a selection was active |
| `Video` | [Video.ts:364-372](packages/lib/src/typescript/lib/component/display/Video.ts#L364-L372) (`setCurrentTime`) | playback position (`currentTime`) | `DOM.sink.setCurrentTime` | `getMediaState()` reads live, never cached | no | `replay` | probe: no `setCurrentTime` write on the rebuilt element. `replayMediaOptions()` (called from `init()`) never replays `currentTime`. manual-verify: a rebuilt `<video>` restarts from position zero |
| `Video` | [Video.ts:379-402](packages/lib/src/typescript/lib/component/display/Video.ts#L379-L402) (`play()`/`pause()`) | whether the media is currently playing | `DOM.sink.mediaPlay`/`mediaPause` | `getMediaState().paused` (live only) | no | `replay` | no field anywhere in `Video.ts` mirrors "is playing"; a release mid-playback leaves the fresh element paused with no way to know it should resume. manual-verify: a `<video>` released while playing does not auto-resume after rebuild |
| `Video` | [Video.ts:260-354](packages/lib/src/typescript/lib/component/display/Video.ts#L260-L354) (`setMuted`/`setVolume`/`setPlaybackRate`) | `muted`/`volume`/`playbackRate` | respective setters | respective getters (`_options.*`) | yes | `accept-loss` | probe: `setMuted`/`setVolume`/`setPlaybackRate` writes all present on the rebuilt element via `replayMediaOptions()`, called from `init()` |
| `Video` | [Video.ts:559-563](packages/lib/src/typescript/lib/component/display/Video.ts#L559-L563) (`attachMediaListeners`) | native, non-bubbling media-event listeners | `init()` → `attachMediaListeners()` | — | yes — `_mediaHandlers` (stable refs) | `accept-loss` | probe: one `addListener` write per media type on the rebuilt element. Video's own `init()` override re-registers these on every render — a materially different survival mechanism than `Event`'s id-routed dispatch, since these are wired via `DOM.sink.addListener` directly (deliberately bypassing `Event`, since these events don't bubble) |
| `VideoPlayer` | [VideoPlayer.ts:454-473](packages/lib/src/typescript/lib/component/display/VideoPlayer.ts#L454-L473) / [:732-743](packages/lib/src/typescript/lib/component/display/VideoPlayer.ts#L732-L743) | fullscreen-active flag + control glyph | browser fullscreen API, reflected via `fullscreenchange` | `isFullscreen()` (`_fullscreen`) | yes | `accept-loss` | re-entering fullscreen requires a live user gesture, so `_fullscreen` genuinely cannot be programmatically replayed after release — this is `accept-loss` by browser design, not convenience. The `fullscreenchange` listener itself **does** survive: `VideoPlayer`'s own `init()` override re-attaches it on every init. manual-verify: exiting fullscreen on the pre-release element and then releasing doesn't leave a stale "fullscreen" glyph — recommend the follow-up plan have the release hook call `syncFullscreen()` post-rebuild |
| `ProgressSpinner` | [ProgressSpinner.ts:195-214](packages/lib/src/typescript/lib/component/display/ProgressSpinner.ts#L195-L214) (`showOverlay`) | the spinner's own element, manually re-parented as a raw child of an *external* component's element | `showOverlay()` | `isOverlay()` (`_overlayTarget !== null`) | partial — `_overlayTarget` names *who*, not the append itself | `replay` | neither `render()` nor `init()` acts on `_overlayTarget`; the append only happens inside `showOverlay()`. Unlike a normal tree child, this component may have no framework parent at all while in overlay mode, so nothing else re-attaches it either — a release while `isOverlay()` is true leaves the fresh element floating, unattached. manual-verify: an overlay spinner reappears attached to its target after a rebuild |
| `CodeEditor` | [CodeEditor.ts:511-579](packages/lib/src/typescript/lib/component/editor/CodeEditor.ts#L511-L579) (`mount()`) | the mounted CodeMirror `EditorView` — text, undo/redo history, selection/caret, scroll position; plus the flash overlay | `mount()`, invoked once via a one-shot `onFirstLayout` | `getValue()` reads `_view.state.doc`; `_options.value` mirrors text only | `_options.value` (text only) | `replay`, structurally broken today | `mount()` guards with `if (this._view) return`, so even if re-invoked after a release it refuses to remount because `_view` still references the destroyed view (see Table B). Nothing re-invokes `mount()` at all today — `onFirstLayout` fires once and is consumed. CodeMirror 6 cleanly separates `EditorState` from `EditorView`, so capturing `_view.state` and constructing `new EditorView({state, parent: freshElement})` would preserve text, undo history, and selection in one step — a stronger recovery path than rebuilding from `_options.value` alone; scroll position (`_view.scrollDOM.scrollTop`/`scrollLeft`) is separate and would need its own capture. manual-verify: undo history and caret position survive a rebuild once a release hook implements this |
| `MarkdownEditor` (`WysiwygSurface`) | [MarkdownEditor.ts:242-260](packages/lib/src/typescript/lib/component/editor/MarkdownEditor.ts#L242-L260) (`mount()`) | the mounted Lexical `contenteditable` DOM binding (not the document — Lexical's editor state is headless) | `mountWysiwyg()`, invoked once via a one-shot `onFirstLayout` | — | N/A — `_editor`'s document state lives independently of the DOM | `replay`, cheap once unblocked | better-positioned than `CodeEditor`: Lexical separates state from view, so text and undo history (`registerHistory`) survive a `WysiwygSurface` element release untouched — `_editor` itself needs no data recovery. Two blockers: `onFirstLayout` is one-shot (never refires), and `mount()`'s own guard `if (editor.getRootElement()) return` refuses to remount because Lexical's internal root-element pointer still references the dead node (`editor.setRootElement(null)` must run first — already done in `dispose()`, but nothing calls it on a mid-life release). manual-verify: WYSIWYG content and undo history are intact after a rebuild once a release hook implements this |

### component/table/, component/list/, component/tree/

| Class | Site | State | Written by | Read by | JS mirror? | Verdict | Notes |
|---|---|---|---|---|---|---|---|
| `DateEditor`/`DateTimeEditor`/`TimeEditor` | [Date.ts:83-92,107-110](packages/lib/src/typescript/lib/component/table/cell/editor/Date.ts#L83-L110), [DateTime.ts:102-129](packages/lib/src/typescript/lib/component/table/cell/editor/DateTime.ts#L102-L129), [Time.ts:85-112](packages/lib/src/typescript/lib/component/table/cell/editor/Time.ts#L85-L112) | current `<input>` text (formatted date / in-progress keystrokes) | `setText`/`onInput` via `DOM.sink.setValue`/`DOM.source.getValue` | `getText`/`getValue` | yes — `_text`/`_value`, kept accurate by the live `"input"` listener | `replay` | the mirror is correct but no `init()` override on these editors or on `TextInputCellEditor` replays `_text` onto a fresh element — capture `_text` pre-release, call `setText(_text)` from an `init()` override. manual-verify: in-progress edit text is preserved across a rebuild |
| `DateEditor` | [Date.ts:203](packages/lib/src/typescript/lib/component/table/cell/editor/Date.ts#L203) | n/a — momentary focus action | `DOM.sink.blur` | — | n/a | `accept-loss` | pure side effect driving the pool's blur-to-commit path; nothing to lose. manual-verify: not applicable — no state is written, so there is nothing a browser check could confirm or refute |
| `Body` (table) | [Body.ts:1179-1184](packages/lib/src/typescript/lib/component/table/Body.ts#L1179-L1184) (`updateRowVisualState`) | pooled `<tr>`'s selection tint + clearing box-shadow | direct `DOM.sink.apply`, deliberately bypassing cached setters | visual only | yes — `_selectedRecords`/`_anchorRecord` | `accept-loss` | ephemeral per-bind tint, reapplied by `updateRowVisualState`/`selectRecord`/`setSelectedRecords`/`renderWindow`. manual-verify: row selection tint after a rebuild |
| `Body` (table) | [Body.ts:1228,1268-1271](packages/lib/src/typescript/lib/component/table/Body.ts#L1228-L1271) (`_updateFocusStyle`) | pooled cell's focus-ring outline | direct `DOM.sink.apply` | visual only | yes — `_anchorRecord`/`_focusedColIndex` | `accept-loss` | reapplied by `_updateFocusStyle`, called after every nav/render pass. manual-verify: focus ring after a rebuild |
| `Row` (table) | [Row.ts:202-208](packages/lib/src/typescript/lib/component/table/Row.ts#L202-L208) (`updateVisualState`) | row background tint (new/dirty/stripe) | direct `DOM.sink.apply` | visual only | yes — `_data`/`_stripe` | `accept-loss` | reapplied by `updateVisualState`, called from `setData`/`Body.bindAndPositionRows`. manual-verify: row tint after a rebuild |
| `Tree` | [Tree.ts:962-970](packages/lib/src/typescript/lib/component/tree/Tree.ts#L962-L970) (`_updateSelectionStyle`) | pooled tree row's selection tint + focus outline | direct `DOM.sink.apply` | visual only | yes — `_selectedNodes`/`_focusNode` | `accept-loss` | reapplied every `renderWindow()` and on every selection change. manual-verify: selection style after a rebuild |
| `TreeRow` | [TreeRow.ts:139-177,258-269](packages/lib/src/typescript/lib/component/tree/TreeRow.ts#L139-L269) (`setRowData`/`init`) | presence of the expand/collapse toggle `Glyph` / loading `ProgressSpinner` (raw children, not via `addComponent`) | `setRowData` appends/removes `_toggle`/`_spinner` | visual only | partial — the fields exist but say only *whether* to show one | `replay` | real gap: unlike the parallel guarded pattern in `list/renderer/Glyph.ts` and `tree/renderer/IconLabel.ts` (both do `if (this._icon) appendChild(...)` in `init()`), `TreeRow.init()` only re-appends `this._renderer` — no `if (this._toggle)`/`if (this._spinner)`. Because `Tree._bindAndMeasure` only calls `setRowData` when the bound index changes, a release+rebuild that doesn't also force a rebind leaves the row toggle/spinner-less until it scrolls out and back. manual-verify: expand/collapse toggle is visible on a row immediately after a rebuild |
| `HeaderCell` | [table/cell/Header.ts:225-260](packages/lib/src/typescript/lib/component/table/cell/Header.ts#L225-L260) (`_mountHeaderGlyph`) | leading header glyph + renderer's left inset | `setHeaderGlyph`/`_mountHeaderGlyph` | visual only | yes — `_headerGlyph` | `accept-loss` | `init()` calls `setHeaderGlyph(this._headerGlyph)` unconditionally when truthy — self-heals on every fresh element. manual-verify: header glyph after a rebuild |
| `HeaderCell` | [table/cell/Header.ts:176-178,410-417](packages/lib/src/typescript/lib/component/table/cell/Header.ts#L176-L417) (`setTooltip`) | attached tooltip text | `Tooltip.attachToElement` | hover only | yes — `_tooltipText` | `accept-loss` | `init()` re-attaches when `_tooltipText` is truthy. manual-verify: header tooltip after a rebuild |
| `SortPriorityBadge` | [SortPriorityBadge.ts:113-119,141-144](packages/lib/src/typescript/lib/component/table/cell/SortPriorityBadge.ts#L113-L144) | badge `textContent` (priority number) | `DOM.sink.apply(element, {text})` | visual only | yes — `_priority` | `accept-loss` | the write lives inside `render()` itself, which reruns automatically on rebuild. manual-verify: badge number after a rebuild |
| `ListItem` | [ListItem.ts:57-69,95-101](packages/lib/src/typescript/lib/component/list/ListItem.ts#L57-L101) | `<li>`'s `data-key` + text content | `DOM.sink.apply` | visual only | yes — `_key`/`_value` | `accept-loss` | `render()` unconditionally re-writes both from the current fields. manual-verify: list item content after a rebuild |
| `AbstractSelectableList` | [AbstractSelectableList.ts:1992-2013](packages/lib/src/typescript/lib/component/list/AbstractSelectableList.ts#L1992-L2013) (`scrollIndexIntoView`, write at 2010/2012) | `_innerPanel`'s vertical scroll offset | direct `DOM.sink.apply(panelEl, {scrollTop})`, bypassing `_innerPanel.setScrollTop()` | n/a | `_innerPanel` is a plain `Component`, so `Component.setScrollTop()` (the only path that updates `_scrollTop`) is available but not used here | `replay` | this bypass makes the `_scrollTop` cache **wrong from the moment it runs**, not merely absent — worse than an unmirrored write. (Even a corrected call through `setScrollTop()` still wouldn't be replayed by base `Component.init()`, which never touches `_scrollTop` on rebuild — the same generic gap as the `Component` scroll row above.) manual-verify: list scroll position after a rebuild |
| `TableHeader` | [table/Header.ts:268-303](packages/lib/src/typescript/lib/component/table/Header.ts#L268-L303) (`getScrollbarCover`) | presence/mount of the scrollbar-cover `<div>` masking the header's right-edge reservation band | lazy-create + `DOM.sink.appendChild` | `Table.ts`'s layout code repositions it | field exists (`_scrollbarCover`) but is never reset to `null` | `replay` | because `getScrollbarCover()` only creates when `this._scrollbarCover === null`, and nothing nulls it on release, post-rebuild it silently returns a dead handle — the cover never gets recreated in the new element (see paired Table B row). manual-verify: scrollbar-cover band is present after a rebuild |

### component/container/, component/chart/, component/diagram/, component/button/, component/shared/

| Class | Site | State | Written by | Read by | JS mirror? | Verdict | Notes |
|---|---|---|---|---|---|---|---|
| `VirtualScroller` (helper, not a `Component`) | [VirtualScroller.ts:55-56](packages/lib/src/typescript/lib/component/container/VirtualScroller.ts#L55-L56) (`_scrollX`/`_scrollY`) | scroll position | `setScrollX`/`setScrollY` | `getScrollX`/`getScrollY`, `clampToContent`, `layoutScrollbars` | yes — own fields | `replay` | `VirtualScroller` is constructed fresh via `new VirtualScroller(owner, element, onScroll)`; it has no "reattach to an existing element" API. If its owner (`Table`/`Tree`, via `VirtualRowView`) reconstructs it on a rebuild rather than reusing the instance, `_scrollX`/`_scrollY` reset to 0 — confirmed concretely via `VirtualRowView.initScroller` below. manual-verify: table/tree scroll position after a rebuild |
| `VirtualRowView` (abstract base for `Table`/`Tree`) | [shared/VirtualRowView.ts:82-90](packages/lib/src/typescript/lib/component/shared/VirtualRowView.ts#L82-L90) (`initScroller`) | the whole `VirtualScroller` instance, hence its scroll position and row pool wiring | `initScroller(element)` — unconditionally `this._scroller = new VirtualScroller(...)`, no guard | `_scroller` used throughout | mirror exists but lives in the discarded object | `replay` | `initScroller` has no `if (this._scroller) return` guard, unlike every other raw-appended-chrome pattern found elsewhere in this sweep. If the owning subclass's `init()` override calls it unconditionally on every render, each rebuild discards the old `VirtualScroller` — and its scroll position — for a fresh one at `(0,0)`. manual-verify: confirm against `Table.ts`/`Tree.ts` (outside this file's scope) whether `initScroller` is actually called unconditionally from an `init()` override |
| `DiagramView` | [DiagramView.ts:567-568,765-766,798-799](packages/lib/src/typescript/lib/component/diagram/DiagramView.ts#L567-L799) | native scroll position (pan/zoom-to-cursor state) | `Component.setScrollLeft`/`setScrollTop` | `getScrollLeft`/`getScrollTop`, `_handleWheel`, `_handlePointerMove` | yes, but not replayed (the generic `Component` gap above) | `replay` | `DiagramView.init()` only wires event listeners, does not restore scroll; since `DiagramView` drives pan/zoom-to-cursor directly off this value, it is functionally meaningful state, not cosmetic — a concrete, high-impact instance of the generic `Component` scroll-cache row. manual-verify: diagram pan position after a rebuild |

---

## Table B — element-derived references a rebuild would leave stale

Two worked rows, verified in the plan and used verbatim:

| Class | Field or map | Site | Points at | Invalidated by |
|---|---|---|---|---|
| `Canvas` | `_ctx` | [Canvas.ts:148](packages/lib/src/typescript/lib/component/display/Canvas.ts#L148) | the 2D context of the released element | nothing today — must be cleared on release |
| `Tooltip` | `elementAttachments` (static `Map<Handle, …>`) | [Tooltip.ts:75](packages/lib/src/typescript/lib/overlay/Tooltip.ts#L75) | the released element's handle | nothing today — the entry outlives the element and the rebuilt element has no tooltip until its owner re-attaches |

| Class | Field or map | Site | Points at | Invalidated by |
|---|---|---|---|---|
| `AnimatedDropdown` (base of `AutoCompleteDropdown`/`TimePickerDropdown`/`AbstractCalendarDropdown`) | `_anchorElement` | [AnimatedDropdown.ts:111](packages/lib/src/typescript/lib/core/AnimatedDropdown.ts#L111), set at [:471](packages/lib/src/typescript/lib/core/AnimatedDropdown.ts#L471) | the trigger element excluded from outside-click-to-close | nothing today — if the trigger's owning component is released and rebuilt, `_anchorElement` keeps pointing at the dead element |
| `FocusHistory` (module-level, not a `Component`) | `_entries: Handle[]` | [FocusHistory.ts:71](packages/lib/src/typescript/lib/core/FocusHistory.ts#L71) | previously-focused elements | already self-healing: every read filters through `DOM.source.isConnected` ([FocusHistory.ts:88](packages/lib/src/typescript/lib/core/FocusHistory.ts#L88)), so a disconnected (released) handle is dropped from the kept list rather than mis-resolved. Listed for completeness, not as a gap |
| `Panel` | `_shadowOverlay` / `_shadowScrollHandler` (`installScrollShadows`) | [Panel.ts:194](packages/lib/src/typescript/lib/core/Panel.ts#L194), guard at [:816-891](packages/lib/src/typescript/lib/core/Panel.ts#L816-L891) | the released element's scroll-shadow overlay child | nothing today except the full `destructor()` path (`removeScrollShadows()`), never a hypothetical release. `installScrollShadows`'s `if (!this._shadowOverlay)` guard means `init()` would believe the overlay already exists and skip recreating it against the fresh element |
| `Panel` | `_overlayScrollElement` / `_scrollbarV` / `_scrollbarH` / `_overlayScrollHandler` (`installOverlayScrollbars`) | [Panel.ts:225](packages/lib/src/typescript/lib/core/Panel.ts#L225), [:1051-1129](packages/lib/src/typescript/lib/core/Panel.ts#L1051-L1129) | the released element's inner scroll `<div>` and its two `Scrollbar` children (appended directly, not via `_components`) | same shape as `_shadowOverlay` above — only `destructor()`'s `removeOverlayScrollbars()` clears these; each field's own `if (!field)` guard in `installOverlayScrollbars`/`init()` means a release that skips this cleanup leaves the overlay-scrollbar structure permanently un-rebuilt |
| `Menu` | `_excludedEl` | [Menu.ts:116](packages/lib/src/typescript/lib/overlay/Menu.ts#L116), set via `setExcludedElement` at [:747](packages/lib/src/typescript/lib/overlay/Menu.ts#L747) | the `MenuBar` button excluded from the outside-click-to-close check | nothing today — if the button's owning component is released and rebuilt, the exclusion silently stops matching |
| `Menu` | `_currentOpener` | [Menu.ts:121](packages/lib/src/typescript/lib/overlay/Menu.ts#L121) | the element that opened the menu | nothing today |
| `Popover` | `_anchorElement` | [Popover.ts:158](packages/lib/src/typescript/lib/overlay/Popover.ts#L158) | the anchor element the popover is positioned against | nothing today — repositioning math would silently use a detached element's stale rect if the anchor's owning component is released and rebuilt |
| `Popover` | `_scrollAncestors` | [Popover.ts:161](packages/lib/src/typescript/lib/overlay/Popover.ts#L161), populated in `attachRepositionListeners` at [:888-892](packages/lib/src/typescript/lib/overlay/Popover.ts#L888-L892) | the anchor's ancestor chain at open time, each with a direct `scroll` listener | nothing today — if the anchor's tree is rebuilt, the listeners on the old (now possibly-detached) ancestors go inert and the new ancestor chain never gets listeners attached |
| `Dialog` | `_previousFocus` | [Dialog.ts:536](packages/lib/src/typescript/lib/overlay/Dialog.ts#L536), set at [:779](packages/lib/src/typescript/lib/overlay/Dialog.ts#L779), read at [:1100-1101](packages/lib/src/typescript/lib/overlay/Dialog.ts#L1100-L1101) | whatever had focus before the dialog opened | nothing today — unlike the global `FocusHistory` list, this single-slot field has no `isConnected` check before `DOM.sink.focus()` is called on it |
| `CodeEditor` | `_view: EditorView \| null` | [CodeEditor.ts:121](packages/lib/src/typescript/lib/component/editor/CodeEditor.ts#L121) | the CodeMirror view bound to the pre-release element's DOM subtree | nothing today; `mount()`'s own `if (this._view) return` guard blocks a fresh mount even if re-invoked (see paired Table A row) |
| `CodeEditor` | `_flashOverlay: Handle \| null` | [CodeEditor.ts:124](packages/lib/src/typescript/lib/component/editor/CodeEditor.ts#L124) | the read-only rejection-wash `<div>` appended to the pre-release host element | same root cause as `_view` — only set inside `mount()`, which never re-runs. `flashReadOnly()` post-release would animate a detached, invisible node — a silent no-op |
| `Markdown` | `_contentHandles: Handle[]` | [Markdown.ts:317](packages/lib/src/typescript/lib/component/display/Markdown.ts#L317) | children of the pre-release root element | `render()` does not call `clearContent()` before rebuilding (unlike `setMarkdown()`, which does) — a release that re-invokes `render()` would append fresh handles onto `_contentHandles` without first clearing the old, now-orphaned entries, accumulating stale handles across every release/rebuild cycle |
| `TableHeader` | `_scrollbarCover: Handle \| null` | [table/Header.ts:54](packages/lib/src/typescript/lib/component/table/Header.ts#L54), declared/used at [:268-303](packages/lib/src/typescript/lib/component/table/Header.ts#L268-L303) | the lazily-created scrollbar-cover `<div>`, a raw child of the header's own element | the header's own element being released/rebuilt — the field is never nulled (see paired Table A row) |
| `VirtualScroller` (helper, not a `Component`) | `_clipBox` | [VirtualScroller.ts:51](packages/lib/src/typescript/lib/component/container/VirtualScroller.ts#L51), created at [:86-89](packages/lib/src/typescript/lib/component/container/VirtualScroller.ts#L86-L89) | a raw `<div>` appended to the owner's element at construction time | `VirtualScroller` has no `init()`/`render()` of its own — nothing re-creates/re-appends this if the owner's root element is swapped without also reconstructing the scroller |
| `VirtualScroller` | `_rowsContainer` | [VirtualScroller.ts:52](packages/lib/src/typescript/lib/component/container/VirtualScroller.ts#L52), created at [:91-94](packages/lib/src/typescript/lib/component/container/VirtualScroller.ts#L91-L94); scroll transform write at [:427](packages/lib/src/typescript/lib/component/container/VirtualScroller.ts#L427) | raw child of `_clipBox` | same as `_clipBox`. Its scroll transform is written via a *raw* `DOM.sink.apply({style:{transform}})` — bypassing `Component`'s tracked `setTranslate`/`_translateX` channel entirely, so it is not auto-replayed even for a plain `Component` |
| `VirtualScroller` | `_shadowOverlay` | [VirtualScroller.ts:60](packages/lib/src/typescript/lib/component/container/VirtualScroller.ts#L60), created at [:104-115](packages/lib/src/typescript/lib/component/container/VirtualScroller.ts#L104-L115) | raw child of `_clipBox` | same as `_clipBox` |
| `VirtualRowView` | `_rowPool: TRow[]` | [shared/VirtualRowView.ts:46](packages/lib/src/typescript/lib/component/shared/VirtualRowView.ts#L46), appended at [:206-231](packages/lib/src/typescript/lib/component/shared/VirtualRowView.ts#L206-L231) | DOM elements parented under the pre-rebuild `VirtualScroller`'s `rowsContainer` | `initScroller` building a brand-new `VirtualScroller` with an empty `rowsContainer` (see paired Table A row). Worse than ordinary staleness: `growRowPool`'s guard `if (!this._scroller \|\| this._rowPool.length >= poolTarget) return` means once the pool has already reached its target size, a post-rebuild `renderWindow()` never re-appends the existing pooled rows — the entire visible row set silently vanishes with no self-healing path |
| `AbstractChart` | `_marks: Array<{parent: Handle, handle: Handle}>` | [chart/AbstractChart.ts:144](packages/lib/src/typescript/lib/component/chart/AbstractChart.ts#L144), populated by `createMark` at [:797-804](packages/lib/src/typescript/lib/component/chart/AbstractChart.ts#L797-L804) | the old `_axisGroup`/`_seriesGroup`/`_overlayGroup` children | `createRootElement()` rebuilds `_svg`/`_axisGroup`/`_seriesGroup`/`_overlayGroup` fresh on every `render()` (self-healing for those four fields) but never clears `_marks` — the next `repaint()`'s `clearMarks()` operates on handles belonging to the old, already-detached group tree. Related: `createMark` never calls `trackHandle`, so live marks also fall outside the base `_ownedHandles` release sweep |
| `ScrollStrip` | `_leadArrow`/`_trailArrow: Button \| null` | [container/ScrollStrip.ts:125-126](packages/lib/src/typescript/lib/component/container/ScrollStrip.ts#L125-L126), raw-appended in `ensureArrows()` at [:489-494](packages/lib/src/typescript/lib/component/container/ScrollStrip.ts#L489-L494) | DOM nodes appended to the pre-rebuild band element | `ScrollStrip.init()` only re-appends `_clip`, never the arrows. Worse: `ensureArrows()`'s idempotency guard `if (this._leadArrow && this._trailArrow) return` means a post-rebuild `layoutArrows()` → `ensureArrows()` sees the still-alive `Button` objects and skips re-appending — the paging arrows are permanently orphaned from the visible tree after the first rebuild |
| `TabButton` | `_closeButton: TabCloseButton \| null` | [button/TabButton.ts:51](packages/lib/src/typescript/lib/component/button/TabButton.ts#L51), raw-appended once at construction in `buildCloseButton()` ([:179](packages/lib/src/typescript/lib/component/button/TabButton.ts#L179)) | DOM node appended to the pre-rebuild button element | `TabButton` has no `init()`/`render()` override at all, nor does its superclass chain (`ToggleButton`'s `render()` override only toggles a class; `Button` has none). `_closeButton` was never added via `addComponent`, so base `Component.init()`'s child-reappend loop never touches it — after a rebuild the close (✕) affordance is simply gone |

---

## Files swept with no DOM-only state

Grouped by directory, matching the sweep order. Each entry's reasoning is one line; full reasoning for anything non-obvious is in the research each directory's sweep produced (retained in this session, summarized here).

**core/**: `Aria.ts` (writes only the buffered `_aria` state that `init()` already replays), `Body.ts` (viewport-size reads only), `Event.ts` (this file *is* the id-keyed dispatch mechanism verified above), `Form.ts` (`requestSubmit` is a transient imperative action, not stored state), `LayerManager.ts` (structural stacking-order appends to `document.body` + geometry `contains` checks, no per-component cache), `PointerDrag.ts` (global body/document cursor chrome during a drag gesture, not any single component's own element), `SmoothScroller.ts` (RAF-driven scroll animation writes through `Component.setScrollLeft`/`setScrollTop`, already covered by that row), `StyleTarget.ts` (this file *is* the inline-style/rule replay buffer other rows rely on), `Theme.ts` (a single global `<style>` element, not per-component), `Util.ts` (measurement reads only).

**overlay/**: `Dock.ts`, `DragFeedback.ts`, `DragGhost.ts`, `DragManager.ts`, `Drawer.ts`, `DropZoneOverlay.ts`, `Notification.ts`, `Rail.ts`, `ReorderIndicator.ts`, `Window.ts` — all either transient drag-time re-parenting (appended to `document.body`/`documentElement` only while a drag/reorder gesture is active, torn down at gesture end), geometry reads for positioning, or global document-level chrome (cursor, body overflow) not owned by any single component's own element.

**layout/**: `CollapseSupport.ts` (RAF-only, no persisted state), `DockRegion.ts` (geometry read only), `Table.ts` (the scrollbar-cover restyle each layout pass is routine — the cover element itself is `TableHeader`'s, catalogued above).

**router/**: `Router.ts` — the `hashchange` listener and `location.hash` reads/writes operate on the window/URL, not any Component's own element; out of scope for a per-Component release API.

**primitive/**: no file in this directory matches either sweep grep (0 results) — confirmed empty by construction, as the plan predicted.

**component/input/**: `AutoCompleteDropdown.ts`, `AutoCompleteField.ts`, `Text.ts`, `TextArea.ts` (replayed by its own `init()` override), `TimePickerDropdown.ts` (delegates all cell state to a child `TimeColumns` component, out of this file).

**component/display/**: `Glyph.ts` (replayed by its own `render()` override), `Glyphs.ts` (module-level singleton sprite, not owned by any single component's lifecycle), `Image.ts` (replayed by its own `render()` override).

**component/table/**: `TableExporter.ts` (the only `DOM.sink` use builds/clicks/removes a transient `<a>` entirely within one synchronous static call — belongs to no instance), `TreeBody.ts` (id-routed click-target resolution only, no `DOM.sink` writes), `cell/editor/CellEditor.ts` (no `DOM.sink`/`DOM.source` calls at all), `cell/editor/TextInputCellEditor.ts` (`type`/`inputmode`/`autocomplete` correctly replayed by its own `init()` override — the intended pattern the `Date`/`DateTime`/`Time` editors above are missing for their text mirror).

**component/list/**: `renderer/Glyph.ts`, `renderer/Label.ts` (both self-heal via a guarded/unconditional re-append in their own `init()`).

**component/tree/**: `renderer/IconLabel.ts`, `renderer/Label.ts` (same self-healing pattern as the list renderers).

**component/container/**: `AccordionIndicator.ts`, `CollapseButton.ts` (both replayed by their own `render()`/id-selector style rules), `DialogBackdrop.ts` (typed setters only), `FieldSet.ts` (legend re-appended unconditionally every `render()`), `Scrollbar.ts` (children added via `addComponent`, self-healing), `SplitGutter.ts` (collapse button re-appended unconditionally every `init()`), `TabBar.ts` (every raw-appended overlay explicitly re-appended in its own `init()` override — confirmed self-healing), `WindowBorder.ts` (replayed by its own `render()` override).

**component/chart/**: `ChartAxis.ts` (pure drawing functions, no component, no state), `ChartLegend.ts` (rows are ordinary `Component`s rebuilt via `addComponent`, self-healing).

**component/diagram/**: `DiagramEdgeLayer.ts` (fully self-healing by design — its own `render()` override calls `rebuildPaths()` after every `super.render()`), `DiagramNode.ts` (`.selected` class replayed by its own `init()` override).

**component/button/**: `MenuButton.ts` (its menu is a standalone overlay, never a raw child of this component's own element), `SplitButton.ts` (chevron added via `addComponent`, self-healing), `ToggleButton.ts` (`.selected` replayed by its own `render()` override).

**component/menubar/**: `ToolBar.ts` (overflow button/spacer added via `addComponent`; overflow menu is a standalone overlay).

---

## Coverage

Re-run at audit time from `packages/lib`:

```
grep -rl 'DOM\.\(sink\|source\)\.' src/typescript/lib/{core,component,overlay,primitive,layout,router}
```
→ **104 files** (`primitive/` contributes 0, confirmed by a separate targeted check).

```
grep -rl 'this\.setElementAttribute(\|this\.removeElementAttribute(' src/typescript/lib
```
→ **8 files**: `component/display/Video.ts`, `component/editor/MarkdownEditor.ts`, `component/input/FileField.ts`, `component/input/TextArea.ts`, `component/input/TextInput.ts`, `component/list/AbstractSelectableList.ts`, `component/table/cell/editor/TextInputCellEditor.ts`, `core/Component.ts` — all 8 already appear in the 104-file list above, so the union is still 104 files. (The plan's drafting-time count was 7; the live count is 8 — `AbstractSelectableList.ts` was added since. Call-site counts also moved: 26 `setElementAttribute` calls, 14 `removeElementAttribute` calls, live-checked at audit time.)

**The attribute class, as one delegated row:** every value written through `Component.setElementAttribute`/`removeElementAttribute` ([Component.ts:1247](packages/lib/src/typescript/lib/core/Component.ts#L1247)) is DOM-only by construction — the seam keeps no cache. The sibling plan `element-attribute-replay-buffer` (`plans/element-attribute-replay-buffer.md`, present on disk at audit time) owns the call-site-by-call-site catalogue for these 8 files. This inventory does not duplicate it, per the plan's Architecture Decision — this paragraph is that row.

**Checklist — every file in the union of the two greps is represented by a row above (Table A and/or Table B) or listed in "Files swept with no DOM-only state":**

- [x] `core/` — 15 of 15 files represented (`Component.ts`, `Event.ts` both additionally serve as the mechanism verified rows cite)
- [x] `overlay/` — 16 of 16 files represented
- [x] `layout/` — 7 of 7 files represented
- [x] `router/` — 1 of 1 file represented
- [x] `primitive/` — 0 files (confirmed empty)
- [x] `component/input/` — 12 of 12 files represented
- [x] `component/display/` — 9 of 9 files represented
- [x] `component/editor/` — 2 of 2 files represented
- [x] `component/table/` — 12 of 12 files represented
- [x] `component/list/` — 4 of 4 files represented
- [x] `component/tree/` — 4 of 4 files represented
- [x] `component/container/` — 10 of 10 files represented
- [x] `component/chart/` — 3 of 3 files represented
- [x] `component/diagram/` — 3 of 3 files represented
- [x] `component/button/` — 4 of 4 files represented
- [x] `component/menubar/` — 1 of 1 file represented
- [x] `component/shared/` — 1 of 1 file represented

Total: 104 of 104 files ticked. 41 Table A rows (3 verbatim + 38 newly catalogued), 22 Table B rows (2 verbatim + 20 newly catalogued) — counted directly from the tables above, not hand-tallied. 7 rows carry `probe:` evidence backed by `packages/lib/tests/component/dom-state-replay-probe.test.ts`; every other Table A row carries a `manual-verify:` note naming the browser check the follow-up plan should run once a release API exists.
