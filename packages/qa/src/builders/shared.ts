// Constants more than one panel uses.

/** What `type` types, cycled: eight ordinary characters that every text target accepts. */
export const TYPE_TEXT = 'measure ';

/** A side pane's width: wide enough for a heading, narrow enough to leave the main pane most of the screen. */
export const SIDE_WIDTH_PX = 240;
