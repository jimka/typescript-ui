<!-- GENERATED by scripts/llms/generate.mjs from scripts/llms/manifest.data.mjs — do not edit by hand -->
# @jimka/typescript-ui — capability manifest for AI agents

> Read this before building any UI feature against this library, so you use what
> exists instead of reinventing it. Open the linked pages for detail.

## Mental model
This is a layout-driven, retained-mode framework — closer to Java Swing than to React or HTML flow. There is no flexbox, no CSS grid, no document flow: every Component is a rectangle absolutely positioned and sized in JavaScript by a LayoutManager in a single doLayout() pass. Construct UIs declaratively with the callable + options-bag idiom — `Panel({ layoutManager: VBox(), components: [Text('Name'), ComboBox()] })`. Theming is token-based; DOM events go through the Event class, semantic events through on/off/emit. Full orientation: docs/guide/mental-model.md.

## Capabilities (use these — do not rebuild them)
Organized by task. Columns: task → symbol · import subpath · summary · docs.

### Layouts
- Stack children vertically → **VBox** · `@jimka/typescript-ui/layout` · A layout manager that places children in a single vertical column. The `mode` option selects between preferred-height sequencing (with weig… · docs/layouts/VBox.md
- Lay children out in a horizontal row → **HBox** · `@jimka/typescript-ui/layout` · A layout manager that places children in a single horizontal row. The `mode` option selects between preferred-width sequencing (with weight… · docs/layouts/HBox.md
- Dock regions around a center (north/south/east/west/center) → **Border** · `@jimka/typescript-ui/layout` · A layout manager that divides a container into five named regions: north, south, east, west, and center. North and south regions span the f… · docs/layouts/Border.md
- Split view with a draggable divider → **Split** · `@jimka/typescript-ui/layout` · A layout manager that splits the container into two or more resizable panels separated by draggable gutter elements. The split orientation… · docs/layouts/Split.md
- Rows-and-columns grid with sized tracks → **Grid** · `@jimka/typescript-ui/layout` · A layout manager that tiles children in a uniform grid of equal-sized cells. Row and column counts can be configured explicitly or left at… · docs/layouts/Grid.md
- Tabbed panels sharing one region → **Tab** · `@jimka/typescript-ui/layout` · A layout manager that renders a row of tab buttons along one edge of the container content area and shows exactly one child component at a… · docs/layouts/Tab.md
- Card stack showing one child at a time → **Card** · `@jimka/typescript-ui/layout` · A layout manager that shows exactly one child component at a time, sizing it to fill the container's inner bounds. The visible child is sel… · docs/layouts/Card.md
- Collapsible accordion sections → **Accordion** · `@jimka/typescript-ui/layout` · A layout manager that stacks vertically collapsible sections, each with a clickable header and an animated content panel. · docs/layouts/Accordion.md
- Stretch a single child to fill the container → **Fit** · `@jimka/typescript-ui/layout` · A layout manager that expects exactly one child component and positions it inside the container's entire inner bounds. The default fill mod… · docs/layouts/Fit.md
- Wrap children like text that reflows (horizontal flow) → **HFlow** · `@jimka/typescript-ui/layout` · A layout manager that packs children in horizontal rows, wrapping to a new line when the next child would exceed the container's inner widt… · docs/layouts/HFlow.md

### Containers / Windows
- Group labelled form fields in a bordered box → **FieldSet** · `@jimka/typescript-ui/component/container` · A fieldset component with an embedded legend title. · docs/components/FieldSet.md
- Auto-build a labelled form from a field descriptor list → **LabeledFieldSet** · `@jimka/typescript-ui/component/container` · A FieldSet whose content is a baseline-aligned form of title/field pairs, generalising the hand-rolled labelled-form pattern from the Bindi… · docs/components/LabeledFieldSet.md
- Line up labelled components without a border/legend → **LabeledGrid** · `@jimka/typescript-ui/component/container` · A chrome-less baseline-aligned title/field grid, rendered in a plain `<div>` with no border and no legend. LabeledFieldSet composes one of… · docs/components/LabeledGrid.md
- Self-managing tab container (add/close/select tabs) → **TabPanel** · `@jimka/typescript-ui/component/container` · A `Container` subclass that owns an internal `Tab` layout manager and exposes a tab-typed factory-accepting `addTab` / `addLazyTab` surface… · docs/components/TabPanel.md
- Self-managing accordion of collapsible sections → **AccordionPanel** · `@jimka/typescript-ui/component/container` · A `Container` subclass that owns an internal `Accordion` layout manager and exposes a section-typed `addSection` surface so consumers do no… · docs/components/AccordionPanel.md
- Bottom status bar with segments → **StatusBar** · `@jimka/typescript-ui/component/container` · A thin horizontal strip mounted at the bottom of a window or panel that surfaces a transient status message and small persistent indicators. · docs/components/StatusBar.md
- Virtualize a huge child list (render only visible rows) → **VirtualScroller** · `@jimka/typescript-ui/component/container` · Shared scroll machinery for transform-based virtual lists (e.g. table body, tree). Owns the rows-container element, two custom Scrollbar ov… · docs/components/VirtualScroller.md
- Mutually-exclusive group of toggle buttons → **ButtonGroup** · `@jimka/typescript-ui/overlay` · Manages mutual exclusivity among a set of RadioButton or ToggleButton instances. · docs/components/ButtonGroup.md
- Corner-pinned floating panel → **FloatingPanel** · `@jimka/typescript-ui/component/container` · A `Panel` that pins itself to one corner of its host's inner box, via an owned AnchorConstraints instance. · docs/components/FloatingPanel.md

### Inputs / Forms
- Semantic <form> container with submit handling → **Form** · `@jimka/typescript-ui/core` · A Panel that bakes the semantic `<form>` tag and wires the native `submit` event to a single `onSubmit` callback. · docs/components/Form.md
- Push button → **Button** · `@jimka/typescript-ui/component/button` · A push button component with a text label and configurable pressed-state appearance. · docs/components/Button.md
- Two-state toggle button → **ToggleButton** · `@jimka/typescript-ui/component/button` · A toggle button component that switches between selected and unselected states on each click. · docs/components/ToggleButton.md
- Button with a primary action plus a dropdown menu → **SplitButton** · `@jimka/typescript-ui/component/button` · A push button with a trailing dropdown chevron. The main button face fires the primary `"action"` event exactly like `Button`; clicking the… · docs/components/SplitButton.md
- Button whose click opens a dropdown menu → **MenuButton** · `@jimka/typescript-ui/component/button` · A push button whose click toggles a rebuild-mode dropdown `Menu` anchored under its bottom-left corner. The menu opens below the button, an… · docs/components/MenuButton.md
- Button whose click opens a custom popup panel → **PopupButton** · `@jimka/typescript-ui/component/button` · A push button whose click toggles a PopupPanel anchored under its bottom-left corner, mirroring `MenuButton` with a `PopupPanel` in place o… · docs/components/PopupButton.md
- Clickable text link that activates in-app (click / Enter) → **Link** · `@jimka/typescript-ui/component/input` · A text link: link-coloured, underlined, and activated by a click or the Enter key. Its **hit area is exactly its text** — it is a Text subc… · docs/components/Link.md
- Checkbox → **Checkbox** · `@jimka/typescript-ui/component/input` · A custom-drawn checkbox rendered as a focusable `<div>` with `role="checkbox"`. · docs/components/Checkbox.md
- Radio button (mutually exclusive within a group) → **RadioButton** · `@jimka/typescript-ui/component/input` · A custom-drawn radio button rendered as a focusable `<div>` with `role="radio"`. The ring + dot is drawn with framework primitives; the nat… · docs/components/RadioButton.md
- On/off switch → **Toggle** · `@jimka/typescript-ui/component/input` · A custom-drawn on/off switch widget rendered as a focusable `<div>` with `role="switch"`. · docs/components/Toggle.md
- Slider for a numeric range → **Slider** · `@jimka/typescript-ui/component/input` · A custom-drawn range slider rendered as a focusable `<div>` with `role="slider"`. · docs/components/Slider.md
- Single-line text field → **TextField** · `@jimka/typescript-ui/component/input` · A single-line text field component backed by an `<input type="text">` element. · docs/components/TextField.md
- Multi-line text area → **TextArea** · `@jimka/typescript-ui/component/input` · A multi-line text area component backed by a `<textarea>` element. · docs/components/TextArea.md
- Masked password field → **PasswordField** · `@jimka/typescript-ui/component/input` · A `TextField` preset that renders an `<input type="password">` element, defaulting `autocomplete="current-password"` and `name="password"`… · docs/components/PasswordField.md
- Username / login-identifier field → **UsernameField** · `@jimka/typescript-ui/component/input` · A username / login-identifier field — a `TextField` preset that defaults `autocomplete="username"` and `name="username"` for browser creden… · docs/components/UsernameField.md
- Numeric input with up/down spinners → **NumberSpinner** · `@jimka/typescript-ui/component/input` · A numeric input field with flanking up/down spin buttons. · docs/components/NumberSpinner.md
- Dropdown select bound to a store → **ComboBox** · `@jimka/typescript-ui/component/input` · A drop-down combo box backed by a styled `<div>` surface and an `AnimatedDropdown` panel. · docs/components/ComboBox.md
- Type-ahead field with autocomplete suggestions → **AutoCompleteField** · `@jimka/typescript-ui/component/input` · A typeahead/autocomplete text field. · docs/components/AutoCompleteField.md
- Date picker field → **DateField** · `@jimka/typescript-ui/component/input` · A date-picker input component. · docs/components/DateField.md
- Combined date-and-time picker field → **DateTimeField** · `@jimka/typescript-ui/component/input` · A date-time-picker input component. · docs/components/DateTimeField.md
- File chooser field → **FileField** · `@jimka/typescript-ui/component/input` · A styled file-selection control: a trigger `Button` that opens the OS file picker, plus a filename label. Wraps a hidden native `<input typ… · docs/components/FileField.md

### Data / Tables / Trees
- Editable data grid / spreadsheet-style table, with per-column or per-cell editor/renderer types → **Table** · `@jimka/typescript-ui/component/table` · A data-bound table component rendered as an HTML `<table>` element. · docs/components/Table.md
- Store-bound table with a scroll frame and toolbar → **TablePanel** · `@jimka/typescript-ui/component/table` · A composite panel that combines a Table with an add/remove/sync toolbar. · docs/components/TablePanel.md
- Table whose rows form an expandable tree → **TreeTable** · `@jimka/typescript-ui/component/table` · A data-bound table whose rows form a parent/child hierarchy. · docs/components/TreeTable.md
- Expandable tree of nodes → **Tree** · `@jimka/typescript-ui/component/tree` · A hierarchical data view with collapsible nodes and virtual scrolling. · docs/components/Tree.md
- Single-select scrollable list → **List** · `@jimka/typescript-ui/component/list` · A scrollable single-selection list box. · docs/components/List.md
- Multi-select list → **MultiSelectList** · `@jimka/typescript-ui/component/list` · A scrollable multi-selection list box. · docs/components/MultiSelectList.md

### Charts / Diagrams
- Line chart over a linear or time x axis → **LineChart** · `@jimka/typescript-ui/component/chart` · A line chart: one path per series over a linear or time x axis and a linear y axis, with optional point markers. The path `d` string is gen… · docs/components/LineChart.md
- Grouped or stacked bar chart over a category axis → **BarChart** · `@jimka/typescript-ui/component/chart` · A bar chart: a discrete band x axis and a linear y axis. Multiple series are drawn grouped (side by side) or stacked. Bar rectangles are ha… · docs/components/BarChart.md
- Clickable series legend for a chart → **ChartLegend** · `@jimka/typescript-ui/component/chart` · A clickable chart legend: one row per series (colour swatch + name). A click on a row emits `"toggle"(seriesIndex)` so the owning chart can… · docs/components/ChartLegend.md
- Auto-laid-out graph / diagram viewer with pan and zoom → **DiagramView** · `@jimka/typescript-ui/component/diagram` · A read-only automatic-layout graph viewer with pan, zoom, and node selection. · docs/components/DiagramView.md

### Display
- Heading / section title text → **Header** · `@jimka/typescript-ui/component/display` · A header bar component containing a left-aligned text label. · docs/components/Header.md
- Image → **Image** · `@jimka/typescript-ui/component/display` · An image component backed by an `<img>` element. · docs/components/Image.md
- Font-Awesome icon glyph → **Glyph** · `@jimka/typescript-ui/component/display` · A small icon rendered from the internal `Glyphs` registry. · docs/components/Glyph.md
- Icon paired with a label → **IconLabel** · `@jimka/typescript-ui/component/display` · A small composite pairing a leading `Glyph` with a trailing `Label`, laid out horizontally with a configurable gap (default 2). · docs/components/IconLabel.md
- Render a Markdown string as formatted content → **Markdown** · `@jimka/typescript-ui/component/display` · A display component that renders a Markdown source string as a live DOM subtree. · docs/components/Markdown.md
- Floating heading-outline minimap → **MarkdownMinimap** · `@jimka/typescript-ui/component/display` · A floating card showing a document's heading outline as a `Tree`. · docs/components/MarkdownMinimap.md
- Markdown viewer with minimap and zoom controls → **MarkdownViewer** · `@jimka/typescript-ui/component/display` · A single-document Markdown viewer with a floating outline minimap and width/zoom controls, both pinned over the prose. · docs/components/MarkdownViewer.md
- Syntax-highlighted code editor with formatting, folding, search, lint and completion → **CodeEditor** · `@jimka/typescript-ui/component/editor` · A syntax-highlighting, formatting code editor wrapping CodeMirror 6. · docs/components/CodeEditor.md
- WYSIWYG rich-text editor whose value is a Markdown string → **MarkdownEditor** · `@jimka/typescript-ui/component/editor` · A WYSIWYG rich-text editor whose public value is a Markdown string, with an optional raw-Markdown source mode. · docs/components/MarkdownEditor.md
- Determinate/indeterminate progress bar → **ProgressBar** · `@jimka/typescript-ui/component/display` · A horizontal progress indicator with a determinate (0–100%) and an indeterminate (continuously animated) mode. · docs/components/ProgressBar.md
- Spinning busy indicator → **ProgressSpinner** · `@jimka/typescript-ui/component/display` · A circular loading indicator rendered as a rotating arc. · docs/components/ProgressSpinner.md
- Page-through pagination controls → **PaginationBar** · `@jimka/typescript-ui/component/display` · A horizontal navigation bar for stepping through pages of a paginated AbstractStore. · docs/components/PaginationBar.md
- Custom 2D drawing surface → **Canvas** · `@jimka/typescript-ui/component/display` · A raster drawing surface backed by a `<canvas>` element and a live `CanvasRenderingContext2D`. · docs/components/Canvas.md
- Custom GPU drawing surface (WebGL2) → **WebGLCanvas** · `@jimka/typescript-ui/component/display` · A GPU drawing surface backed by a `<canvas>` element and a live `WebGL2RenderingContext`. · docs/components/WebGLCanvas.md
- Bare native video surface → **Video** · `@jimka/typescript-ui/component/display` · A native `<video>` surface primitive — the media twin of `Image`. · docs/components/Video.md
- Video player with a themable control bar → **VideoPlayer** · `@jimka/typescript-ui/component/display` · A video player: a native `<video>` surface (Video) framed by a control bar built entirely from this library's own components — play / pause… · docs/components/VideoPlayer.md
- Bulleted (unordered) list of items → **BulletedList** · `@jimka/typescript-ui/component/list` · An unordered (bulleted) list component. · docs/components/BulletedList.md
- Numbered (ordered) list of items → **NumberedList** · `@jimka/typescript-ui/component/list` · An ordered (numbered) list component. · docs/components/NumberedList.md

### Overlays
- Floating draggable/resizable window → **Window** · `@jimka/typescript-ui/overlay` · A floating, resizable, and draggable window component. · docs/components/Window.md
- Window whose body is a tab container → **TabWindow** · `@jimka/typescript-ui/overlay` · A floating, resizable, draggable window whose interior *is* a Tab layout — no separate title-bar header. The tab strip serves as both the w… · docs/components/TabWindow.md
- Modal dialog (alert/confirm/prompt/custom) → **Dialog** · `@jimka/typescript-ui/overlay` · A modal dialog component with a title bar, scrollable content area, and button row. · docs/components/Dialog.md
- Edge-anchored sliding drawer → **Drawer** · `@jimka/typescript-ui/overlay` · An edge-anchored panel that rests off-screen against a viewport edge and slides into view when opened, overlaying the rest of the UI. · docs/components/Drawer.md
- Context / dropdown menu → **Menu** · `@jimka/typescript-ui/overlay` · A floating menu panel that operates in one of two modes: · docs/components/Menu.md
- Checkbox row in a menu → **CheckboxMenuRow** · `@jimka/typescript-ui/component/container` · A `Menu` row hosting a real `Checkbox`, for a multi-select menu. · docs/components/CheckboxMenuRow.md
- Radio row in a menu → **RadioMenuRow** · `@jimka/typescript-ui/component/container` · A `Menu` row hosting a real `RadioButton`, for a single-choice menu. · docs/components/RadioMenuRow.md
- Anchored floating popover → **Popover** · `@jimka/typescript-ui/overlay` · An anchored, non-modal floating bubble with a directional arrow tail. Use `Popover` for click-triggered, interactive content (title, body,… · docs/components/Popover.md
- Custom popup panel anchored to a trigger → **PopupPanel** · `@jimka/typescript-ui/overlay` · A floating panel that sizes itself to its content, places itself against a trigger rect, and caps its height to the room available there —… · docs/components/PopupPanel.md
- Hover tooltip → **Tooltip** · `@jimka/typescript-ui/overlay` · A singleton floating tooltip that appears near the cursor after a short delay. · docs/components/Tooltip.md
- Transient toast notification → **Notification** · `@jimka/typescript-ui/overlay` · A lightweight toast-style notification that appears in the bottom-right corner of the viewport and auto-dismisses after a configurable dura… · docs/components/Notification.md
- Browse / re-open past notifications → **NotificationHistoryButton** · `@jimka/typescript-ui/overlay` · A trigger button that opens a menu of recent notifications. The menu lists the in-session Notification history in chronological order (late… · docs/components/NotificationHistoryButton.md
- Live runtime diagnostics window (FPS, heap, components, layout, listeners, CSS rules) → **DiagnosticsOverlay** · `@jimka/typescript-ui/diagnostics` · A floating window showing live runtime diagnostics: browser-level numbers (FPS, JS heap, DOM node count, long tasks) beside framework-inter… · docs/components/DiagnosticsOverlay.md
- Stylesheet duplicate-rule audit window (per-instance rule dedup by wasted bytes) → **StyleAuditOverlay** · `@jimka/typescript-ui/diagnostics` · A floating window showing the stylesheet-dedup audit — the same view embedded in the demo app's own "Style Audit" tab, opened alongside Dia… · docs/components/StyleAuditOverlay.md

### Data layer
- In-memory record collection backing a table/list → **Store** · `@jimka/typescript-ui/data` · A general-purpose concrete store that pairs a Model with an optional Proxy. Use this class when you do not need a dedicated store subclass. · docs/data/store.md
- Store that loads/saves rows over a REST endpoint → **AjaxStore** · `@jimka/typescript-ui/data` · A store backed by an AjaxProxy that talks to a remote HTTP/REST endpoint. Convenience subclass that constructs the proxy from an AjaxProxyO… · docs/data/store.md
- Typed record schema (fields, defaults, validation) → **Model** · `@jimka/typescript-ui/data` · A concrete, configurable model created at runtime from a field array. Use this class when you do not need a dedicated model subclass. · docs/data/model.md
- A single editable record instance → **ModelRecord** · `@jimka/typescript-ui/data` · A single data record managed by a store. · docs/data/record.md
- Declare a typed field on a model → **Field** · `@jimka/typescript-ui/data` · Represents a single typed field in a model's schema. Encapsulates the field's name, type, default value, raw-data mapping, description, and… · docs/data/model.md
- Two-way bind a component property to a record field → **Binding** · `@jimka/typescript-ui/core` · Synchronises a ModelRecord with a set of UI components. · docs/data/binding.md
- Hierarchical store for tree data → **TreeStore** · `@jimka/typescript-ui/data` · A store that manages records arranged in a parent/child hierarchy. Each record names its parent's id via TreeStoreOptions.parentField; root… · docs/data/store.md
- Node in a tree store → **TreeNode** · `@jimka/typescript-ui/data` · A node in a TreeStore's hierarchy: a thin structural wrapper around a ModelRecord, carrying parent/child links, depth, and leaf state. The… · docs/data/store.md
- Relate models (has-many / belongs-to) → **Association** · `@jimka/typescript-ui/data` · Declarative association descriptor; behaviour lives in AbstractModel / ModelRecord / AbstractStore. · docs/data/associations.md

### App shell
- Map the URL to a top-level app section → **Router** · `@jimka/typescript-ui/router` · Maps the URL hash or path to a single top-level app section. Patterns are registered with register (e.g. `"/data/rows/:sel"`); on a navigat… · docs/concepts/routing.md
- Give the app a browser-tab icon → **Favicon** · `@jimka/typescript-ui/core` · Installs the browser-tab icon by appending a `<link rel="icon">` to `<head>`. · docs/components/Body.md

## Conventions (hard rules)
1. Never use CSS flex/grid or `position: relative` — arrange children with a LayoutManager (HBox/VBox/Border/Grid/Split/…). → docs/concepts/layout-system.md
2. Size with setMinSize/setPreferredSize/setMaxSize honouring min ≤ preferred ≤ max; to scroll, wrap content in a Panel with autoScroll — never `overflow` CSS. → docs/concepts/sizing.md
3. Construct with the callable + options-bag idiom (`Button({ text })`), not post-construction setters; reserve `setX` for runtime changes. → docs/recipes/component-options.md
4. Mount the top-level layout with `Body.init({ layoutManager, components })` — one call; use `Body.getInstance()` only to reach the body afterwards. → docs/components/Body.md
5. Route DOM events through the `Event` class (`Event.addListener`); emit and observe semantic events with `on`/`off`/`emit`. Never call `addEventListener`. → docs/concepts/events.md
6. Theme through design tokens, never hardcoded colours. → docs/concepts/theming.md
7. Move data through Model/Store/Binding, not manual DOM writes — bind components to records instead of pushing values by hand. → docs/concepts/data-binding.md
8. One DOM element per component class; touch the element only at render time, not from a setter dispatched during construction. → docs/concepts/component-lifecycle.md

## Drill down
- Component & layout detail (options, examples) → docs/components/, docs/layouts/
- Full generated API reference → docs/api/
- Framework internals & binding rules → ARCHITECTURE.md

## Developing the library
Building the library itself (not just apps against it): read ARCHITECTURE.md (event surfaces, one-DOM-element-per-class, absolute positioning, the size-constraint contract, typed-setter rules) and CODE_CONVENTIONS.md. Concepts index: docs/concepts/.
